import Loader from "./Loader";

const LoaderTest = () => {
    return (
        <Loader
            style={{
                position: "absolute",
                zIndex: "1",
            }}
        />
    );
}

export default LoaderTest;
