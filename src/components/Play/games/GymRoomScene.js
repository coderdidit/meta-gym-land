import Phaser from "phaser";

export default class GymRoom extends Phaser.Scene {
    constructor() {
        super({ key: 'gym-main-room' });
    }

    preload() { }

    create() { }

    update(time, delta) { }
}
